name: voice_monitor_app
description: Voice monitor app (foreground only).
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.3.1
  noise_meter: ^3.1.0
  vibration: ^1.8.4

flutter:
  uses-material-design: true
